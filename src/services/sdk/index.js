import { 
  flameWager, 
  switchNetwork, 
  currentNetwork, 
  initPools, 
  destroySubscription,
  connect,
  disconnect,
  initializeGraphQL,
  getContractAddresses,
} from "./flameWager"
import analytics from "./analytics"

export { 
  flameWager, 
  switchNetwork, 
  currentNetwork, 
  initPools, 
  destroySubscription, 
  connect,
  disconnect,
  initializeGraphQL,
  getContractAddresses,
  analytics,
}
