import { flameWager, currentNetwork, switchNetwork, initPools, destroySubscription } from "./flameWager"
// import { withdrawAll } from "./withdraw"
// import { fetchBalance } from "./misc"
import analytics from "./analytics"

export { flameWager, analytics, currentNetwork, switchNetwork, initPools, destroySubscription }